<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="URMS.WinUI.Controls.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Margin="0"
    Background="Transparent">

  <Grid Height="72" Margin="0" Padding="0">
    <Border Background="Transparent" BorderThickness="0" Padding="0" Margin="0" CornerRadius="0" Translation="0,0,16">
      <Border.Shadow>
        <ThemeShadow/>
      </Border.Shadow>
      <Grid>
        <Grid.Background>
          <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF0A0F18" Offset="0"/>
            <GradientStop Color="#FF0D121C" Offset="1"/>
          </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Rectangle IsHitTestVisible="False" Opacity="0.03">
          <Rectangle.Fill>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
              <GradientStop Color="#55FFFFFF" Offset="0.0"/>
              <GradientStop Color="#14000000" Offset="0.25"/>
              <GradientStop Color="#33FFFFFF" Offset="0.5"/>
              <GradientStop Color="#14000000" Offset="0.75"/>
              <GradientStop Color="#55FFFFFF" Offset="1.0"/>
            </LinearGradientBrush>
          </Rectangle.Fill>
        </Rectangle>

        <!-- Logo -->
        <Grid Grid.Column="0" Width="48" Height="48" Margin="16,0,16,0" VerticalAlignment="Center">
          <Ellipse Width="48" Height="48" Fill="#5500CFFF" Opacity="0.25" IsHitTestVisible="False"/>
          <Image Width="48" Height="48" VerticalAlignment="Center"
                 Stretch="Uniform" Source="ms-appx:///Assets/images/urms_logo.png"/>
        </Grid>

        <!-- Title + Subtitle + Spectrum -->
        <Grid Grid.Column="1" VerticalAlignment="Center">
          <StackPanel VerticalAlignment="Center" Spacing="0" Margin="8,0,0,0">
            <TextBlock Text="URMS" FontFamily="Consolas" FontSize="23" FontWeight="ExtraBold"
                       CharacterSpacing="280" Foreground="#FFB9EFFF"/>
            <TextBlock Text="Unified Resource Management System"
                       FontFamily="Consolas" FontSize="11"
                       CharacterSpacing="100" Foreground="#FF8899BB" Margin="0,-2,0,0"/>
          </StackPanel>
          <StackPanel x:Name="SpectrumPanel" Orientation="Horizontal"
                      VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="16" Margin="0,0,0,2"/>
        </Grid>

        <!-- Status + Radar -->
        <StackPanel Grid.Column="3" Orientation="Vertical" VerticalAlignment="Center" Spacing="0" Margin="0,0,24,0">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="6">
            <Ellipse x:Name="StatusDot" Width="6" Height="6" Fill="#FF00E0FF" VerticalAlignment="Center"/>
            <TextBlock x:Name="TxtStatus" Text="SCAN_DONE" FontFamily="Consolas" FontSize="10"
                       CharacterSpacing="80" Foreground="#FF00E0FF" VerticalAlignment="Center"/>
          </StackPanel>
          <Canvas Width="32" Height="32" VerticalAlignment="Center" Margin="0,2,0,0">
            <Ellipse Canvas.Left="0" Canvas.Top="0" Width="32" Height="32" Stroke="#2600E0FF" StrokeThickness="1"/>
            <Path Data="M16,16 L30,16 A16,16 0 0 0 28,10 Z" Fill="#1A00E0FF">
              <Path.RenderTransform>
                <RotateTransform x:Name="RadarRotate" CenterX="16" CenterY="16"/>
              </Path.RenderTransform>
            </Path>
            <Ellipse x:Name="RadarBlip0" Canvas.Left="20" Canvas.Top="9" Width="3" Height="3" Fill="#CC00E0FF" Opacity="0"/>
            <Ellipse x:Name="RadarBlip1" Canvas.Left="9" Canvas.Top="21" Width="2.5" Height="2.5" Fill="#9900E0FF" Opacity="0"/>
          </Canvas>
        </StackPanel>

        <!-- Date + Clock -->
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="24" Margin="0,0,24,0" HorizontalAlignment="Right">
          <TextBlock x:Name="TxtDate" Text="yyyy/MM/dd (ddd)" FontFamily="Consolas" FontSize="16" Foreground="#FF00E0FF" VerticalAlignment="Center"/>
          <TextBlock x:Name="TxtClock" Text="HH:mm:ss" FontFamily="Consolas" FontSize="16" FontWeight="Bold" Foreground="#FF00E0FF" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Window Control Buttons -->
        <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0" Spacing="24">
          <Grid>
            <Button x:Name="MinimizeButton" Width="28" Height="28" MinWidth="28" MinHeight="28" Padding="0"
                    Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                    CornerRadius="2"
                    Click="OnMinimizeClicked">
              <Button.Resources>
                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#22FFFFFF"/>
                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#22FFFFFF"/>
              </Button.Resources>
              <FontIcon Glyph="&#xE921;" FontSize="14" Foreground="#FFB9EFFF"/>
            </Button>
          </Grid>
          <Grid>
            <Button x:Name="MaximizeButton" Width="28" Height="28" MinWidth="28" MinHeight="28" Padding="0"
                    Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                    CornerRadius="2"
                    Click="OnMaximizeClicked">
              <Button.Resources>
                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#22FFFFFF"/>
                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#22FFFFFF"/>
              </Button.Resources>
              <FontIcon Glyph="&#xE922;" FontSize="14" Foreground="#FFB9EFFF"/>
            </Button>
            <Button x:Name="RestoreButton" Width="28" Height="28" MinWidth="28" MinHeight="28" Padding="0"
                    Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                    CornerRadius="2"
                    Click="OnRestoreClicked">
              <Button.Resources>
                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#22FFFFFF"/>
                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#22FFFFFF"/>
              </Button.Resources>
              <FontIcon Glyph="&#xE923;" FontSize="14" Foreground="#FFB9EFFF"/>
            </Button>
          </Grid>
          <Grid>
            <Button x:Name="CloseButton" Width="28" Height="28" MinWidth="28" MinHeight="28" Padding="0"
                    Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                    CornerRadius="2"
                    Click="OnCloseClicked">
              <Button.Resources>
                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#22FFFFFF"/>
                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#22FFFFFF"/>
              </Button.Resources>
              <FontIcon Glyph="&#xE8BB;" FontSize="14" Foreground="#FFB9EFFF"/>
            </Button>
          </Grid>
        </StackPanel>

        <Rectangle VerticalAlignment="Bottom" Height="2" Fill="#FF00E8FF"/>
        <Rectangle VerticalAlignment="Bottom" Height="6" Margin="0,2,0,0" IsHitTestVisible="False" Opacity="0.15">
          <Rectangle.Fill>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
              <GradientStop Color="#6600E8FF" Offset="0"/>
              <GradientStop Color="#00000000" Offset="1"/>
            </LinearGradientBrush>
          </Rectangle.Fill>
        </Rectangle>
      </Grid>
    </Border>
  </Grid>
</UserControl>
