*** Begin Patch
*** Update File: .github/workflows/triage-dashboard-e2e-selfheal.yml
@@
-on:
-  push:
-    branches: [ main ]
-  schedule:
-    - cron: "0 18 * * *"
-  workflow_dispatch:
+on:
+  push:
+    branches: [ main ]
+  schedule:
+    - cron: "0 18 * * *"
+  workflow_dispatch:
+    inputs:
+      AUTO_FIX_MODE:
+        description: 'Auto fix mode for self-heal runs'
+        required: false
+        default: 'dry-run'
@@
-permissions:
-  contents: read
-  actions: read
+permissions:
+  contents: write
+  actions: read
@@
       - name: Diagnostic - write meta
         run: |
           echo "diag: ${{ github.sha }} ${{ github.event_name }}" > diag.txt
           echo "workflow_path: .github/workflows/triage-dashboard-e2e-selfheal.yml" >> diag.txt
+      - name: Diagnostic - create validation ready marker
+        run: |
+          mkdir -p .github/actions-runs || true
+          echo "validation_ready: ${{ github.sha }}" > .github/actions-runs/validation_ready.txt
*** End Patch
